k8s_repo_url: "https://pkgs.k8s.io/core:/stable:/v{{ k8s_version }}/deb/"
k8s_prereqs:
  - apt-transport-https
  - ca-certificates
  - curl
  - gpg
k8s_packages:
  - "kubelet={{ k8s_version }}.*"
  - "kubeadm={{ k8s_version }}.*"
  - "kubectl={{ k8s_version }}.*"
